import {primary} from "../colors"

export const primaryInteraction = {
  background: "#ffffff",
  borderWidth: "2px",
  borderStyle: "solid",
  borderColor: primary,
  borderRadius: 3,
  textDecoration: "none",
  padding: 15
}
