{
  "name": "www",
  "description": "The frontend for lacqueristas",
  "scripts": {
  },
  "env": {
    "API_HOST": {
      "required": true
    },
    "API_MEDIATYPE": {
      "required": true
    },
    "API_PORT": {
      "required": true
    },
    "API_PROTOCOL": {
      "required": true
    }
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
