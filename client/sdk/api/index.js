const PROTOCOL = "http"
const HOST = "localhost"
const PORT = "8080"
const VERSION = "v1"
const MEDIATYPE = "application/lacqueristas.api+json; api-verison=1"

export {
  PROTOCOL,
  HOST,
  PORT,
  VERSION,
  MEDIATYPE
}
